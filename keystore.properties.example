# 复制本文件为 keystore.properties 并填写你的签名信息（不要提交到版本库）。
# 生成签名密钥（在 JDK 的 bin 目录下执行）：
#   keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias autoclicker
#
# STORE_FILE=../my-release-key.jks
# STORE_PASSWORD=你的密钥库密码
# KEY_ALIAS=autoclicker
# KEY_PASSWORD=你的密钥密码
